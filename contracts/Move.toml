[package]
name = "self-paying-agent"
edition = "legacy"
license = "MIT"

[dependencies]
# Using published framework - no git dependency
Sui = { local = false, address = "0x2" }

[addresses]
agent = "0x0"

[dev-dependencies]

[dev-addresses]